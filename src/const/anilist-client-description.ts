const description = `
anilist-client is a type-safe, chainable GraphQL client designed to simplify
interaction with the AniList API by abstracting raw query construction into a declarative,
builder-style interface.
<br/>
<br/>
Instead of manually writing GraphQL queries and handling request payloads, 
developers can fluently compose queries using method chaining and field selection objects. 
The library internally translates these structured calls into optimized GraphQL queries, 
handles variable injection, and returns normalized JSON responses.`;

export default description;
